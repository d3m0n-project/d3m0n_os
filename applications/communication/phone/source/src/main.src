# d3m0n source

Window.OnCreate =>
	original_x = 76;
	x = original_x;
	y = 140;
	delta = 60;
	i = 1;
	while (i < 13)
	{
		template.load("layouts/dial_num.template", x, y, i);
		
		x = x + delta;
		if (x > 205)
		{
			x = original_x;
			y = y + delta;
		}
		i++;
	}