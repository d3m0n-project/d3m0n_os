# d3m0n source

Window.OnCreate =>
  log("Loading app launcher...", "orange");
  row = 0;
  col = 0;
  for (name, icon) in apps.list()
  {
    x_pos = 10 + (75 * row);
    y_pos = 30 + (col * 95);
    template.load("/apps/system/desktop/source/layouts/appview.template", x_pos, y_pos, icon, name);
    row++;
    if (row > 2)
    {
      row = 0;
      col++;
    }
  }