#include <stdio.h>
#include <string.h>
#include "SettingsFileHandler.h"

int main(int argc, char *argv[])
{
    getSetting("name", "./text.txt");
    // init display

    // load apps in memory

    // loads apps launcher
    //    load topbar
    //    load background

    // load splashscreen
    // load apps in grid

    // hide splashscreen

    return 0;
}
